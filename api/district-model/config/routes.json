{
  "routes": [
    {
      "method": "GET",
      "path": "/district-models",
      "handler": "district-model.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/district-models/count",
      "handler": "district-model.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/district-models/:id",
      "handler": "district-model.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/district-models",
      "handler": "district-model.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/district-models/:id",
      "handler": "district-model.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/district-models/:id",
      "handler": "district-model.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
